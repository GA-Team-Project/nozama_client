const store = require('../store')
let firstName

store.user.first_name.length > 0 ?
    firstName = store.user.first_name : firstName = "User"


const navTemplate = `<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Hello, ${firstName}
        <span class="caret"></span>
    </a>
<ul class="dropdown-menu">
    <li>
        <h3>My Account</h3>
        <hr>
    </li>
    <li>
    <a href="#" id="my-orders">My Orders</a>
    </li>
    <li role="separator" class="divider"></li>
    <li>
    <a href="#" id="change-pw">Change Password</a>
    </li>
    <li role="separator" class="divider"></li>
    <li>
    <a href="#" id="sign-out">Sign Out</a>
    </li>`

const changePassTemplate = `
<form id="change-password" class="border">
  <input type="password" name="passwords[old]" placeholder="Current Password" required>
  <input type="password" name="passwords[new]" placeholder="New Password" required>
  <input type="submit" value="Change Password">
</form>
`

module.exports = {
    navTemplate,
    changePassTemplate
}